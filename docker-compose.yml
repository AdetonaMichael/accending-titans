services:
  acceding-titans-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acceding-titans-web
    restart: unless-stopped
    ports:
      - "3000:3000"      # optional: keep for direct debugging
    env_file:
      - .env.local
